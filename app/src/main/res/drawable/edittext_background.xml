<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="50dp"/> <!-- 50% của chiều rộng/cao để tạo thành viền tròn -->

    <!-- Màu nền -->
    <solid android:color="@android:color/white"/> <!-- Màu đỏ -->

    <!-- Đường viền -->
    <stroke
        android:width="10px"
        android:color="@color/red"/>
</shape>